# Project list:

## Pro 01

1. Generate a 5*5 Gaussian template, apply it to the image for Gaussian convolution, and output the image.
2. Compile integral graph algorithm, filter the input image with mean value of three scales, and output the image.

## Pro 02

1. Write a function to evaluate the rotation invariance of Harris corner detection algorithm.

## Pro 03

1. 自己生成带噪声的500个数据，包含一个线性模型，用Ransac鲁棒估计算法估计出模型参数，并输出。

1. Generate 500 noisy data, including a linear model, estimate model parameters with Ransac robust estimation algorithm, and output.

